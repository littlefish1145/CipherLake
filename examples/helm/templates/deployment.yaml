apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "cipherlake.fullname" . }}
  labels:
    {{- include "cipherlake.labels" . | nindent 4 }}
    app.kubernetes.io/component: gateway
spec:
  replicas: {{ .Values.gateway.replicas }}
  selector:
    matchLabels:
      {{- include "cipherlake.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: gateway
  template:
    metadata:
      labels:
        {{- include "cipherlake.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: gateway
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
        checksum/secret: {{ include (print $.Template.BasePath "/secret.yaml") . | sha256sum }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        runAsNonRoot: {{ .Values.security.runAsNonRoot }}
        runAsUser: {{ .Values.security.runAsUser }}
        fsGroup: {{ .Values.security.fsGroup }}
      containers:
        - name: cipherlake
          image: "{{ .Values.gateway.image.repository }}:{{ .Values.gateway.image.tag }}"
          imagePullPolicy: {{ .Values.gateway.image.pullPolicy }}
          command: ["/usr/local/bin/cipherlake"]
          args:
            - "--config"
            - "/etc/cipherlake/config.yaml"
          ports:
            - name: s3
              containerPort: 9000
              protocol: TCP
            - name: admin
              containerPort: 9001
              protocol: TCP
            - name: metrics
              containerPort: 9091
              protocol: TCP
          env:
            - name: CIPHERLAKE_GATEWAY_ACCESS_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ include "cipherlake.fullname" . }}-credentials
                  key: accessKey
            - name: CIPHERLAKE_GATEWAY_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ include "cipherlake.fullname" . }}-credentials
                  key: secretKey
          volumeMounts:
            - name: config
              mountPath: /etc/cipherlake
              readOnly: true
            - name: data
              mountPath: /var/lib/cipherlake/data
            - name: metadata
              mountPath: /var/lib/cipherlake/metadata
          livenessProbe:
            httpGet:
              path: /health
              port: s3
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /ready
              port: s3
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            {{- toYaml .Values.gateway.resources | nindent 12 }}
      volumes:
        - name: config
          configMap:
            name: {{ include "cipherlake.fullname" . }}-config
        - name: data
          {{- if .Values.storage.persistence.enabled }}
          persistentVolumeClaim:
            claimName: {{ include "cipherlake.fullname" . }}-data
          {{- else }}
          emptyDir: {}
          {{- end }}
        - name: metadata
          {{- if .Values.metadata.persistence.enabled }}
          persistentVolumeClaim:
            claimName: {{ include "cipherlake.fullname" . }}-metadata
          {{- else }}
          emptyDir: {}
          {{- end }}

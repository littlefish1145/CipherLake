apiVersion: v1
kind: Secret
metadata:
  name: {{ include "cipherlake.fullname" . }}-credentials
  labels:
    {{- include "cipherlake.labels" . | nindent 4 }}
type: Opaque
stringData:
  accessKey: {{ .Values.gateway.accessKey | quote }}
  secretKey: {{ .Values.gateway.secretKey | quote }}
